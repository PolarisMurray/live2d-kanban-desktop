# 如何运行这个程序

## ⚠️ 重要说明

**这个项目目前不能直接双击运行**，因为：

1. ❌ 没有编译好的 `.app` 应用包
2. ❌ 没有 Xcode 项目文件（`.xcodeproj`）
3. ✅ 只有源代码文件，需要先编译

**必须使用 Xcode 先构建项目，然后才能运行。**

---

## 🚀 最快运行方法（推荐）

### 步骤 1：打开 Xcode

```bash
# 在终端中进入项目目录
cd /Users/alex/github/live2d-kanban-desktop

# 打开 Xcode（会自动识别 Swift 项目）
open -a Xcode .
```

或者手动：
1. 打开 **Xcode**
2. **File → Open** (⌘O)
3. 选择项目目录

### 步骤 2：创建 Xcode 项目

由于项目只有源代码，需要创建 Xcode 项目：

1. **File → New → Project** (⌘⇧N)
2. 选择 **macOS** → **App**
3. 填写信息：
   - **Product Name**: `AILearningCompanion`
   - **Team**: 选择你的开发团队
   - **Organization Identifier**: `com.yourcompany`
   - **Interface**: **SwiftUI**
   - **Language**: **Swift**
   - **Storage**: None
4. 点击 **Next**，选择项目目录，点击 **Create**

### 步骤 3：添加源代码

1. 在 Xcode 项目导航器中，**右键点击项目名称**
2. 选择 **"Add Files to AILearningCompanion..."**
3. 选择 **`Sources`** 文件夹
4. 确保：
   - ✅ **Create groups**
   - ✅ **Add to targets: AILearningCompanion**
   - ❌ **取消勾选 "Copy items if needed"**
5. 点击 **Add**

### 步骤 4：设置应用入口

1. 打开 Xcode 自动创建的 `AILearningCompanionApp.swift`
2. 替换为以下代码：

```swift
import SwiftUI

@main
struct AILearningCompanionApp: App {
    @StateObject private var themeManager = ThemeManager()
    @StateObject private var appState = AppState()
    
    var body: some Scene {
        WindowGroup {
            MainView()
                .environmentObject(themeManager)
                .environmentObject(appState)
                .preferredColorScheme(themeManager.colorScheme)
        }
        #if os(macOS)
        .windowStyle(.automatic)
        .defaultSize(width: 1000, height: 700)
        #endif
        .commands {
            CommandGroup(replacing: .newItem) {}
            
            CommandMenu("学习") {
                Button("新建学习会话") {}
                    .keyboardShortcut("n", modifiers: .command)
                
                Button("生成闪卡") {}
                    .keyboardShortcut("f", modifiers: .command)
            }
        }
        
        #if os(macOS)
        Settings {
            SettingsView()
                .environmentObject(themeManager)
                .environmentObject(appState)
        }
        #endif
    }
}
```

### 步骤 5：配置构建设置

1. 选择项目 → 选择 **AILearningCompanion** 目标
2. **General** 标签：
   - **Minimum Deployments**: macOS 13.0
3. **Signing & Capabilities** 标签：
   - 启用 **App Sandbox**
   - 添加：
     - ✅ **Outgoing Connections (Client)**
     - ✅ **User Selected File (Read/Write)**

### 步骤 6：运行！

1. 在 Xcode 顶部选择 **"My Mac"** 作为运行目标
2. 按 **⌘R** 或点击运行按钮
3. 应用会启动！

---

## 📝 详细指南

- **完整设置步骤**：查看 [XCODE_SETUP.md](./XCODE_SETUP.md)
- **中文运行指南**：查看 [运行指南.md](./运行指南.md)

---

## ⚡ 快速检查清单

运行前确保：
- ✅ Xcode 已安装
- ✅ macOS 13.0 或更高版本
- ✅ 所有 `Sources` 文件已添加到 Xcode 项目
- ✅ 应用入口点已正确设置
- ✅ 最低部署版本设置为 macOS 13.0
- ✅ App Sandbox 已配置

---

## 🆘 遇到问题？

### 编译错误
- 检查所有文件是否添加到目标
- 确保 Swift 版本是 5.9+

### 运行错误
- 检查控制台错误信息
- 验证权限设置

### 需要帮助
- 查看 [XCODE_SETUP.md](./XCODE_SETUP.md)
- 查看 [运行指南.md](./运行指南.md)

---

**总结**：这个项目需要先在 Xcode 中创建项目并构建，然后才能运行。大约需要 5-10 分钟设置。

