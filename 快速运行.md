# 快速运行指南

## ⚡ 最快运行方法（推荐）

### 方法一：使用 Xcode（5 分钟）

1. **打开终端，进入项目目录**：
   ```bash
   cd /Users/alex/github/live2d-kanban-desktop
   ```

2. **在 Xcode 中打开项目**：
   ```bash
   open -a Xcode .
   ```
   或者手动打开 Xcode，然后 File → Open → 选择项目目录

3. **创建 Xcode 项目**（如果还没有）：
   - File → New → Project (⌘⇧N)
   - 选择 **macOS** → **App**
   - 产品名称：`AILearningCompanion`
   - 界面：**SwiftUI**
   - 语言：**Swift**
   - 保存位置：选择当前项目目录

4. **添加源代码**：
   - 右键项目 → Add Files to "AILearningCompanion"...
   - 选择 `Sources` 文件夹
   - ✅ 添加到目标
   - ❌ 取消勾选 "Copy items if needed"

5. **设置应用入口**：
   - 打开 `AILearningCompanionApp.swift`（Xcode 自动创建的文件）
   - 替换为：
   ```swift
   import SwiftUI
   
   @main
   struct AILearningCompanionApp: App {
       @StateObject private var themeManager = ThemeManager()
       @StateObject private var appState = AppState()
       
       var body: some Scene {
           WindowGroup {
               MainView()
                   .environmentObject(themeManager)
                   .environmentObject(appState)
                   .preferredColorScheme(themeManager.colorScheme)
           }
           #if os(macOS)
           .windowStyle(.automatic)
           .defaultSize(width: 1000, height: 700)
           #endif
           .commands {
               CommandGroup(replacing: .newItem) {}
               
               CommandMenu("学习") {
                   Button("新建学习会话") {
                   }
                   .keyboardShortcut("n", modifiers: .command)
                   
                   Button("生成闪卡") {
                   }
                   .keyboardShortcut("f", modifiers: .command)
               }
           }
           
           #if os(macOS)
           Settings {
               SettingsView()
                   .environmentObject(themeManager)
                   .environmentObject(appState)
           }
           #endif
       }
   }
   ```

6. **运行**：按 **⌘R**

---

## 🚫 不能直接运行的原因

**这个项目目前不能直接双击运行**，因为：

1. ❌ 没有 `.app` 应用包（需要先构建）
2. ❌ 没有 `.xcodeproj` 项目文件（需要创建）
3. ✅ 只有源代码文件（需要编译）

**必须使用 Xcode 或命令行工具先构建，然后才能运行。**

---

## 🔧 其他运行方法

### 方法二：使用 Swift Package Manager（命令行）

```bash
# 1. 进入项目目录
cd /Users/alex/github/live2d-kanban-desktop

# 2. 构建项目
swift build

# 注意：SPM 只能构建库，不能直接创建 .app
# 要运行应用，仍需要使用 Xcode
```

### 方法三：一键创建 Xcode 项目脚本

我可以为你创建一个自动化脚本，自动设置 Xcode 项目。需要我创建吗？

---

## ✅ 运行后的下一步

1. **配置 API 密钥**：
   - 打开应用
   - 按 ⌘, 打开设置
   - 输入 OpenAI API 密钥

2. **加载 Live2D 模型**（可选）：
   - 将模型文件放在 `Resources/Models/`
   - 或在设置中指定路径

3. **开始使用**：
   - 点击 "Ask AI" 开始对话
   - 使用其他功能按钮

---

## 📝 总结

**当前状态**：✅ 源代码完整，❌ 需要构建才能运行

**最快运行**：使用 Xcode 创建项目并运行（约 5 分钟）

**详细步骤**：查看 [XCODE_SETUP.md](./XCODE_SETUP.md) 或 [运行指南.md](./运行指南.md)

